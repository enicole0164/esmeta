def ToBigInt64(
  argument: Unknown,
): Normal[BigInt] | Abrupt {
  call %0 = clo<ToBigInt>(argument)
  let n = [? %0]
  let int64bit = (% ([math] n) (** 2 64))
  if (! (< int64bit (** 2 63))) {
    return ([bigInt] (- int64bit (** 2 64)))
  } else {
    return ([bigInt] int64bit)
  }
}