def <SYNTAX>:TemplateMiddleList[0,0].Evaluation(
  this: Ast:TemplateMiddleList[0,0],
): Unknown {
  sdo-call %0 = this[0]->TV()
  let head = %0
  sdo-call %1 = this[1]->Evaluation()
  let subRef = %1
  call %2 = clo<GetValue>(subRef)
  let sub = [? %2]
  call %3 = clo<ToString>(sub)
  let middle = [? %3]
  return (concat head middle)
}